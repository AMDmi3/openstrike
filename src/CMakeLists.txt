SET(SOURCES
	main.cc
)

INCLUDE_DIRECTORIES(${PROJECT_SOURCE_DIR}/lib ${SDL2PP_INCLUDE_DIRS})
ADD_EXECUTABLE(openstrike ${SOURCES})
TARGET_LINK_LIBRARIES(openstrike graphics game gameobjects dat ${SDL2PP_LIBRARIES})
